#ifndef WacomTabletDriver_h
#define WacomTabletDriver_h

#include <Availability.h>
#include <DriverKit/IOService.iig>
#include <HIDDriverKit/IOUserHIDEventService.iig>

class WacomTabletDriver : public IOUserHIDEventService
{
public:
    virtual bool init() override;
    virtual void free() override;

    virtual kern_return_t Start(IOService * provider) override;
    virtual kern_return_t Stop(IOService * provider) override;

protected:
    virtual bool handleStart(IOService * provider) LOCALONLY;
    virtual void handleReport(uint64_t    timestamp,
                              uint8_t   * report,
                              uint32_t    reportLength,
                              IOHIDReportType type,
                              uint32_t    reportID) LOCALONLY;
};

#endif /* WacomTabletDriver_h */
